# BlackBoard/helm/templates/secret-gitlab.yaml
# GitLab token -- requires existingSecret when enabled (no placeholder fallback)
{{- if .Values.gitlab.enabled }}
{{- if not .Values.gitlab.existingSecret }}
{{- fail "gitlab.enabled=true requires gitlab.existingSecret to be set (pre-created secret with 'host' and 'token' keys)" }}
{{- end }}
{{- end }}
