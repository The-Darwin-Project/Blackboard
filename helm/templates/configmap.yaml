# BlackBoard/helm/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    {{- include "darwin.labels" . | nindent 4 }}
data:
  # GCP Configuration
  GCP_PROJECT: {{ .Values.gcp.project | default "not-configured" | quote }}
  GCP_LOCATION: "{{ .Values.gcp.location }}"
  
  # LLM Configuration (per-component)
  LLM_PROVIDER: "{{ .Values.gcp.provider }}"
  # Brain
  LLM_MODEL_BRAIN: "{{ .Values.gcp.llm.brain.model }}"
  LLM_TEMPERATURE_BRAIN: "{{ .Values.gcp.llm.brain.temperature }}"
  LLM_MAX_TOKENS_BRAIN: "{{ .Values.gcp.llm.brain.maxOutputTokens }}"
  # Aligner
  LLM_MODEL_ALIGNER: "{{ .Values.gcp.llm.aligner.model }}"
  LLM_TEMPERATURE_ALIGNER: "{{ .Values.gcp.llm.aligner.temperature }}"
  LLM_THINKING_ALIGNER: "{{ .Values.gcp.llm.aligner.thinkingLevel }}"
  LLM_MAX_TOKENS_ALIGNER: "{{ .Values.gcp.llm.aligner.maxOutputTokens }}"
  # Archivist (event summarization for deep memory)
  LLM_MODEL_ARCHIVIST: "{{ .Values.gcp.llm.archivist.model }}"
  LLM_TEMPERATURE_ARCHIVIST: "{{ .Values.gcp.llm.archivist.temperature }}"
  LLM_THINKING_ARCHIVIST: "{{ .Values.gcp.llm.archivist.thinkingLevel }}"
  LLM_MAX_TOKENS_ARCHIVIST: "{{ .Values.gcp.llm.archivist.maxOutputTokens }}"
  # Lesson Extractor (Claude via Vertex AI for document analysis)
  LLM_MODEL_LESSON_EXTRACTOR: "{{ .Values.gcp.llm.lessonExtractor.model }}"
  # Headhunter (MR triage classification)
  LLM_MODEL_HEADHUNTER: "{{ .Values.gcp.llm.headhunter.model }}"
  LLM_TEMPERATURE_HEADHUNTER: "{{ .Values.gcp.llm.headhunter.temperature }}"
  LLM_THINKING_HEADHUNTER: "{{ .Values.gcp.llm.headhunter.thinkingLevel }}"
  LLM_MAX_TOKENS_HEADHUNTER: "{{ .Values.gcp.llm.headhunter.maxOutputTokens }}"
  # TimeKeeper (instruction refinement)
  LLM_MODEL_TIMEKEEPER: "{{ .Values.gcp.llm.timekeeper.model }}"
  LLM_TEMPERATURE_TIMEKEEPER: "{{ .Values.gcp.llm.timekeeper.temperature }}"
  LLM_MAX_TOKENS_TIMEKEEPER: "{{ .Values.gcp.llm.timekeeper.maxOutputTokens }}"
  # Nightwatcher (shift consolidation)
  LLM_MODEL_NIGHTWATCHER: "{{ .Values.gcp.llm.nightwatcher.model }}"
  LLM_TEMPERATURE_NIGHTWATCHER: "{{ .Values.gcp.llm.nightwatcher.temperature }}"
  LLM_THINKING_NIGHTWATCHER: "{{ .Values.gcp.llm.nightwatcher.thinkingLevel }}"
  LLM_MAX_TOKENS_NIGHTWATCHER: "{{ .Values.gcp.llm.nightwatcher.maxOutputTokens }}"
  # Quota rate limiter
  LLM_TPM_LIMIT: "{{ .Values.gcp.tpmLimit }}"
  
  # Archivist Lesson Quality Thresholds
  LESSON_DEDUP_THRESHOLD: "{{ .Values.archivist.lessonDedupThreshold }}"
  LESSON_DEDUP_SEARCH_LIMIT: "{{ .Values.archivist.lessonDedupLimit }}"
  LESSON_RECALL_OVERFETCH: "{{ .Values.archivist.lessonRecallOverfetch }}"
  LESSON_RECALL_SCORE_FLOOR: "{{ .Values.archivist.lessonRecallScoreFloor }}"
  LESSON_RECALL_MAX_EXPERIENCE: "{{ .Values.archivist.lessonRecallMaxExperience }}"
  KNOWLEDGE_DEDUP_THRESHOLD: "{{ .Values.archivist.knowledgeDedupThreshold }}"
  KNOWLEDGE_DEDUP_SEARCH_LIMIT: "{{ .Values.archivist.knowledgeDedupLimit }}"
  
  # Brain Progressive Skills
  BRAIN_PROGRESSIVE_SKILLS: "{{ .Values.app.progressiveSkills }}"
  
  # Application Info
  APP_NAME: "{{ .Values.app.name }}"
  APP_VERSION: "{{ .Values.app.version }}"
  
  # AI Transparency & Compliance
  DARWIN_CONTACT_EMAIL: "{{ .Values.app.contactEmail }}"
  DARWIN_FEEDBACK_FORM_URL: "{{ .Values.app.feedbackFormUrl }}"
