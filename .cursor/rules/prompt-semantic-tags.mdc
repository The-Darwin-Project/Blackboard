---
description: Prompt semantic tag vocabulary -- reusable template for XML tag differentiation in agent system instructions
alwaysApply: false
globs: "**/jarvis_instructions.py,**/brain_skills/**/*.md,**/nightwatcher_prompt.py"
---

# Prompt Semantic Tag Template

## Pattern: `<{agent}_{type} id="{name}">`

Five-type vocabulary with fixed compliance gradient:

| Type | Compliance | LLM Interpretation | Use for |
|---|---|---|---|
| `rule` | Highest | Hard constraint -- violating feels wrong | Boundaries, prohibitions, invariants |
| `protocol` | High | Procedural sequence -- steps to follow | Workflows, decision trees, handoffs |
| `mode` | Medium-high | Behavioral state -- act like X when Y | Input-activated persona switching |
| `skill` | Medium | Contextual capability -- know-how for this situation | Phase-dependent procedures, progressive loading (BrainSkillLoader agents only) |
| `context` | Low | Reference material -- inform, don't direct | Taxonomies, format docs, shared knowledge |

## Naming Convention

- Tag: `<{agent}_{type} id="{section-name}">`
- {agent} = agent identity (jarvis, friday, architect, sysadmin)
- {type} = one of: rule, protocol, mode, skill, context
- {section-name} = kebab-case section identifier

## Folder Convention (for BrainSkillLoader agents)

- always/ → {agent}_rule (always-on constraints)
- source/ → {agent}_rule (per-source behavioral constraints)
- context/ → {agent}_context (environment, reference)
- Progressive phase folders → {agent}_skill (contextual capabilities)

## Scaling

Adding a new compliance level (e.g., `guideline` between protocol and mode) is a vocabulary extension, not a structural change. Update this rule file and apply to affected agents.

## Structural Invariant

Every opening `<{agent}_{type} id="...">` MUST have a matching closing `</{agent}_{type}>` with the same type. The CI test `test_event_source_consistency.py::test_jarvis_tag_pairs_structural` enforces this for JARVIS. Extend for FRIDAY when semantic differentiation ships.

## FRIDAY Variant: `{type}_section` Naming

FRIDAY uses a simplified naming pattern without the agent prefix:

| Tag | Resolved from | Count |
|---|---|---|
| `rule_section` | `always/` or `source/` folder default | 17 |
| `skill_section` | All other folders (dispatch, triage, etc.) | 18 |
| `context_section` | `context/` folder default | 6 |
| `protocol_section` | Frontmatter override `tag_type: protocol_section` | 4 |

### Resolution Order

1. **Frontmatter override**: `tag_type: protocol_section` in YAML frontmatter (validated against `_VALID_TAG_TYPES` allowlist)
2. **Folder default**: `_FOLDER_TAG_TYPE` mapping (`always` -> rule, `source` -> rule, `context` -> context)
3. **Fallback**: `"skill_section"` for all unmapped folders

### Why Not `friday_rule`?

FRIDAY's tags use `{type}_section` (e.g., `rule_section`) rather than the `{agent}_{type}` pattern used by JARVIS (`jarvis_rule`). This is because FRIDAY's skills are loaded dynamically by BrainSkillLoader -- the agent prefix adds no disambiguation value when there's only one consumer. Contributors MUST NOT use `friday_rule` in frontmatter; the `_VALID_TAG_TYPES` allowlist silently rejects it and falls back to the folder default.

### Allowlist

```python
_VALID_TAG_TYPES = frozenset({"rule_section", "skill_section", "protocol_section", "context_section"})
```
