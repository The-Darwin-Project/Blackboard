---
description: "Protocol for structuring MR descriptions so Darwin's Headhunter can parse Bot Instructions and generate execution plans."
alwaysApply: true
---

# Bot MR Description Protocol

When building automations that create GitLab Merge Requests targeting Darwin (the Headhunter polls these via `/todos`), structure the MR description so Darwin can parse it deterministically without an LLM call.

## Required Format

The description must contain three parts in order:

### 1. Type Header

A single `##` line identifying the MR type. Known types that trigger deterministic Tier 1 parsing:

- `## Submodule Update`
- `## Konflux Release`

Unknown headers fall through to Tier 2 (LLM analysis) but are still useful as context.

### 2. Metadata Table

A markdown table with `| Field | Value |` rows. Include all relevant identifiers so the Brain and agents have structured context:

```markdown
| Field | Value |
|---|---|
| **Submodule** | kubevirt-plugin |
| **Version** | v4.22 |
| **Promotion** | promo-abc123 |
```

### 3. Bot Instructions Block

Must start with `### Bot Instructions`. Contains two required sections:

- `**On pipeline success**:` -- what Darwin should do when the MR pipeline passes.
- `**On pipeline failure**:` -- numbered steps for Darwin to follow when the pipeline fails.

## Parsing Contract

- Headhunter's `_parse_bot_instructions()` detects `### Bot Instructions` in the MR description (truncated to 2000 characters).
- If the marker is present, a YAML plan is generated directly from the instructions (no LLM call).
- If parsing fails or the marker is absent, the MR falls through to Tier 2 LLM analysis with the full description as context.
- The `mr_description` is also passed to the Brain via `evidence.gitlab_context.mr_description`.

## Reference: Submodule Update

```markdown
## Submodule Update

| Field | Value |
|---|---|
| **Submodule** | {SUBMODULE_NAME} |
| **Upstream Commit** | {COMMIT_SHA} |
| **Version** | {VERSION} |
| **Branch** | {FEATURE_BRANCH} -> {TARGET_BRANCH} |
| **Promotion** | {PROMOTION_ID} |
| **Application** | {APPLICATION} |

---

### Bot Instructions

This MR was auto-generated by the submodule updater
for `{SUBMODULE_NAME}` on version `{VERSION}`.

**On pipeline success**: Merge this MR and notify maintainers.

**On pipeline failure**:
1. Retest the pipeline once.
2. If the pipeline passes after retest, merge and notify maintainers.
3. If the pipeline fails again, close this MR and notify maintainers
   with the pipeline failure details.
```

## Reference: Konflux Release

```markdown
## Konflux Release

| Field | Value |
|---|---|
| **Bundle** | {BUNDLE_VERSION} |
| **Snapshot** | {SNAPSHOT_ID} |
| **Chart** | {CHART_VERSION} |
| **Promotion** | {PROMOTION_ID} |
| **Project** | {PROJECT} |
| **Stage** | {STAGE} |

---

### Bot Instructions

This MR was auto-generated by Kargo stage `{STAGE}`
in project `{PROJECT}`.
It contains rendered Konflux Release CRs for bundle
`{BUNDLE_VERSION}`.

**On pipeline success**: Merge this MR.

**On pipeline failure**:
1. Close this MR.
2. Notify the maintainers about the failure with the
   pipeline logs and failure reason.
```
